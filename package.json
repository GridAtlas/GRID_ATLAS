{
  "name": "grid-atlas",
  "version": "0.1.0",
  "private": true,
  "description": "Grid-only point atlas web app prototype.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "check": "node --check src/main.js && node --check scripts/dev-server.mjs"
  }
}

