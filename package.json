{
  "name": "grid-atlas",
  "version": "0.1.0",
  "private": true,
  "description": "Grid-only point atlas web app prototype.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "cloud:dev": "wrangler dev",
    "cloud:types": "wrangler types cloud/worker-configuration.d.ts",
    "cloud:dry-run": "wrangler deploy --dry-run --env=\"\" --outdir dist/cloud-worker",
    "test": "vitest run",
    "check": "node --check src/main.js && node --check src/cloud-client.js && node --check src/gridatlas-import.js && node --check src/gridatlas-assets.js && node --check service-worker.js && node --check scripts/dev-server.mjs && node scripts/check-gridatlas-spec.mjs && node --check cloud/worker.js && node --check cloud/auth.js && node --check vitest.config.mjs && vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.8",
    "@cloudflare/workers-types": "^5.20260727.1",
    "@types/node": "^26.1.1",
    "vitest": "^4.1.10",
    "wrangler": "^4.114.0"
  },
  "dependencies": {
    "fflate": "^0.8.3"
  }
}
