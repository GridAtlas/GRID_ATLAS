# GRID ATLAS

建物名、地名、地形、道路を表示しない、格子と登録地点だけの地図アプリです。

## 現在のブートストラップ内容

- 格子だけのキャンバス表示
- 緯度経度ベースの地点登録、写真、コメント保存
- 地点同士の線引き
- 地球上の2点間直線距離の計測
- 複数地点の巡回順提案
- 共有URL/テキストからの地点登録補助
- 選択地点を外部地図アプリで開く操作
- PWA共有ターゲット設定
- PC向けの距離分析パネル
- スマホ向けの地点登録フォーム
- LocalStorage保存
- JSONエクスポート、インポート
- 依存パッケージなしのローカル開発サーバー

## 起動

```powershell
npm run dev
```

既定では `http://127.0.0.1:5177/` で起動します。

## 検証

```powershell
npm run check
```

## 仕様書

[docs/development-spec.md](docs/development-spec.md)

## 注意

この初期版は地球上の緯度経度に対応したプロトタイプとしてブラウザ内に保存します。Googleマップ等の短縮URLだけでは、ブラウザ単体で展開できない場合があります。スマホとPC間の自動同期、認証、写真ストレージ、複数ワークスペース管理は、次フェーズでAPIとデータベースを追加して実装します。

