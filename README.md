# GRID ATLAS

建物名、地名、地形、道路を表示しない、格子と登録地点だけの地図アプリです。

## 現在のブートストラップ内容

- 格子だけのキャンバス表示
- 地点の登録、写真、コメント、座標保存
- 地点同士の線引き
- 直線距離の計測
- PC向けの距離分析パネル
- スマホ向けの地点登録フォーム
- LocalStorage保存
- JSONエクスポート、インポート
- 依存パッケージなしのローカル開発サーバー

## 起動

```powershell
npm run dev
```

既定では `http://127.0.0.1:5177/` で起動します。

## 検証

```powershell
npm run check
```

## 仕様書

[docs/development-spec.md](docs/development-spec.md)

## 注意

この初期版はプロトタイプとしてブラウザ内に保存します。スマホとPC間の自動同期、認証、写真ストレージ、複数ワークスペース管理は、次フェーズでAPIとデータベースを追加して実装します。

